#include <iostream>
using namespace std;

int main() {
    int a = 10, b = 5;

    cout << a + b << endl;        // Arithmetic
    cout << (a > b) << endl;      // Relational
    cout << (a && b) << endl;     // Logical
    cout << (a & b) << endl;      // Bitwise

    return 0;
}
